[pytest]
filterwarnings =
    # Suppress experimental Google GenAI SDK deprecation warnings
    ignore::DeprecationWarning:google.genai.*
markers =
    integration: marks live/external API integration tests (deselect with '-m "not integration"')
    slow: marks tests as slow running